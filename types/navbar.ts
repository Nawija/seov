export type MenuBurgerProps = {
  handleShowMenu: () => void;
  showMenu: boolean;
};
export type ClassNameProps = {
  className: string;
};

export type SearchProps = {
  id: string;
  img: string;
  title: string;
  price: number;
  prevPrice: number;
};
